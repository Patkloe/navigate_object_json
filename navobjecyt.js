var test = { 
               a: "nouv",
               b: "json",
               c: "decouvrir"
            };
var getkeys = Object.keys(test);
for(var i of getkeys)
    alert("keys of the object : " + " " + i);
 
